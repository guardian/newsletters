{
	"name": "newsletters",
	"version": "1.0.0",
	"description": "Newsletters API",
	"repository": "git@github.com:guardian/newsletters.git",
	"private": true,
	"scripts": {
		"postinstall": "yarn --cwd v0 && yarn --cwd v1",
		"prepare": "husky install"
	},
	"devDependencies": {
		"husky": "^8.0.2"
	}
}
